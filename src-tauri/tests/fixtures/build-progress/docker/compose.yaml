services:
  success:
    build:
      context: .
      target: success
      network: none
  failure:
    profiles: [failure-test]
    build:
      context: .
      target: failure
      network: none
