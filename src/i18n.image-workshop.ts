export const imageWorkshopChinese: Record<string, string> = {
  'Selected cases override the coding Agent image and command. Project dependency composition and the default Pi image do not replace these custom coding images. Knowledge generation still uses the experiment Agent. Custom command token usage and enforcement are unavailable.': '所选用例已覆盖编码 Agent 的镜像和命令，项目依赖组合与默认 Pi 镜像不会替换它们。知识库生成仍使用实验中的 Agent。自定义命令的 Token 用量未知，工具无法强制其 Token 上限。',
  'Remove incompatible Pi arguments': '清除不适用的 Pi 启动参数',
  'Coding Agent · separate from test commands': '编码 Agent · 与测试命令分开配置',
  'Agent launch mode': 'Agent 启动方式',
  'Use the experiment Agent (Pi by default)': '使用实验中配置的 Agent（默认 Pi）',
  'Run my own Agent command': '执行我自己的 Agent 命令',
  'Custom Agent image': '自定义 Agent 镜像',
  'Agent command format': 'Agent 命令格式',
  'Agent startup command': 'Agent 启动命令',
  'Custom Agent configuration contract': '自定义 Agent 如何接收任务和配置',
  'This overrides coding only. Knowledge generation and constraint roles keep their experiment configuration. The image is used as-is: install your Agent, python3, git and /bin/sh; do not include answers, hidden tests or credentials.': '仅覆盖此用例的编码 Agent；知识库生成和约束评估仍使用实验配置。镜像按原样运行，需预装自己的 Agent、python3、git 和 /bin/sh，不得包含答案、隐藏测试或凭据。',
  'The exact step prompt is in CTXBENCH_PROMPT_FILE. Read it as a quoted argument or redirect it to stdin. Commands run in /workspace and must exit when finished. Exit 0 completes coding; only the separate evaluator decides PASS.': '每一步的原始提示词保存在 CTXBENCH_PROMPT_FILE 指向的文件中；以带引号的参数读取它，或重定向到标准输入。命令在 /workspace 中运行，完成后必须退出。退出码 0 只表示编码完成，是否通过由独立的评测步骤判定。',
  'CTXBENCH_MODEL, CTXBENCH_PROVIDER, CTXBENCH_THINKING and CTXBENCH_MAX_TOKENS expose the selected settings. Your command must apply them; the tool cannot verify an arbitrary CLI model or enforce its token limit. Use runtime environment variables for API keys, not command literals.': 'CTXBENCH_MODEL、CTXBENCH_PROVIDER、CTXBENCH_THINKING、CTXBENCH_MAX_TOKENS 提供实验中选定的配置。启动命令须自行使用这些值；工具无法确认任意 CLI 实际用了什么模型，也无法强制它的 Token 上限。API Key 通过运行时环境变量传入，不要直接写进命令。',
  'Missing token usage does not block custom commands or affect grading. These commands bypass shared token accounting; only metered roles remain budget-protected. Token totals and allowances exclude custom commands, not their actual consumption. Configure spending limits in your Agent or Provider.': '缺少 Token 用量不影响自定义命令执行和测试判分。这些命令跳过共享 Token 记账与额度检查，预算仅保护可统计用量的角色。显示的 Token 总量和配置额度不包含自定义命令，不代表它们没有消耗；请在 Agent 或 Provider 侧设置消费限额。',
  'Container time, CPU, memory and network limits still apply. Each workflow step starts a new command with its own prompt. Put custom Agent arguments in this command, not in global Pi startup arguments.': '容器时长、CPU、内存和网络限制仍有效。编排中每一步用独立提示词启动新命令。自定义 Agent 的参数请写在此命令中，不要使用全局 Pi 启动参数。',
  'Install dependencies and non-secret defaults under /opt or /etc. /workspace is replaced by the clean baseline. Use a writable HOME for UID 10001; otherwise a temporary HOME is used. Image ENTRYPOINT and CMD are replaced by the command adapter.': '依赖和非敏感默认配置放在 /opt 或 /etc 等目录，/workspace 会被干净的基线代码替换。HOME 应允许 UID 10001 写入，否则使用临时 HOME。镜像原有 ENTRYPOINT 和 CMD 会被命令适配器替换。',
  'Provide the custom Agent image.': '请填写自定义 Agent 镜像。',
  'Provide a valid custom Agent command.': '请填写有效的自定义 Agent 命令。',
  'Pull an existing image': '拉取现成镜像',
  'Remote image reference': '远端镜像完整地址',
  'Pull image': '下载镜像',
  'Download an existing Linux amd64 image from Docker Hub or your company registry. Enter the full image reference, not a docker pull command. Existing local tags are reused, never replaced; use a new tag or digest for a newer version.': '从 Docker Hub 或公司镜像仓下载 Linux amd64 镜像。填写完整镜像地址，不要粘贴 docker pull 命令。本地已有同名镜像会直接复用，不覆盖；需要新版时请指定新标签或摘要。',
  'I trust this image source and allow the download and disk usage. Pulling does not run the image or call a model.': '我信任此镜像来源，允许下载并占用磁盘空间。下载不会启动镜像或调用模型。',
  'The evaluation service must be running. Private registry login is separate from model API keys. If authentication is required, docker login and pull this image in the selected WSL distribution, then refresh local images. Docker Hub access is not required when a full company address is supplied.': '需要先启动评测服务。私有镜像仓登录与模型 API Key 是两回事。若提示认证失败，请在选定的 WSL 发行版中执行 docker login 并拉取该镜像，然后刷新本地列表。填写公司完整地址时不需要访问 Docker Hub。',
  'Build your image step by step': '一步步制作自己的镜像',
  'Prepare a dependency-only image once and reuse it. The original image is never edited. Do not copy the repository, answers, hidden tests, API keys or login state into image layers.': '一次准备好依赖环境，多次复用；不会修改原镜像。不要把代码仓、参考答案、隐藏测试、API Key 或登录状态写进镜像层。',
  'Image recipe steps': '镜像制作步骤',
  'Choose a base': '选择基础镜像',
  'Install dependencies': '安装依赖',
  'Configure defaults': '添加默认配置',
  'Review recipe': '检查构建方案',
  'Choose a Linux image that already has your language runtime. The templates below assume Debian/Ubuntu; Alpine, distroless and other systems need their own package commands. Pull the base first; the build pins the installed image ID.': '选择已包含项目语言运行时的 Linux 镜像。以下模板适用于 Debian/Ubuntu；Alpine、distroless 等系统需要对应的安装方式。先下载基础镜像，构建时会固定其本地镜像 ID。',
  'Dependency command template': '依赖安装模板',
  'Append a template': '追加一个模板',
  'Custom Agent prerequisites': '自定义 Agent 必备工具',
  'Dependency and configuration commands': '安装依赖、修改配置的命令',
  'Use shell commands, not FROM or RUN. Replace example packages and company registry URLs. Files uploaded in the next step are available under /opt/company before these commands run. Use pip --index-url, npm --registry, or Maven settings.xml for company sources.': '填写 Shell 命令，不要写 FROM 或 RUN。请替换示例包名和公司源地址。下一步上传的文件会先放到 /opt/company，再执行这些命令。公司源可通过 pip --index-url、npm --registry 或 Maven settings.xml 配置。',
  'Install packages under /opt/venv or system paths, never only under /workspace. Install your own Agent here, for example npm install -g your-package --registry=https://npm.company.example. Templates do not know your real package name.': '将依赖安装到 /opt/venv 或系统目录，不要只装在 /workspace。自己的 Agent 也在这里安装，例如 npm install -g your-package --registry=https://npm.company.example。模板中的包名需要自行替换。',
  'Non-secret configuration files': '上传非敏感配置文件',
  'Image defaults (NAME=value, one per line)': '镜像默认环境变量（每行 NAME=value）',
  'For example MY_AGENT_CONFIG=/opt/company/settings.json. To replace /etc configuration, add cp or sed commands in the previous step. Do not include tokens in JSON, Maven settings or environment defaults. API keys belong in Runtime environment, supplied at execution only.': '例如 MY_AGENT_CONFIG=/opt/company/settings.json。如需覆盖 /etc 下的默认配置，可在上一步添加 cp 或 sed 命令。JSON、Maven settings 和默认环境变量均不得包含令牌。API Key 在“运行时环境变量”中配置，仅运行时传入。',
  'The benchmark runs as UID 10001, even if the image default user is root. Keep Agent configuration readable by this user and HOME writable. /workspace is the fresh baseline, not an image configuration directory.': '评测时以 UID 10001 运行，即使镜像默认用户是 root。请确保该用户能读取 Agent 配置，并能写入 HOME。/workspace 用于干净的代码基线，不要把镜像配置放在那里。',
  'Use recipe in the build form': '将方案填入构建表单',
  'Next: review and confirm the build below. After success copy the new image tag into the case test image or custom Agent image, run a small case, then export the image for your intranet. A successful build alone does not prove Agent or test compatibility.': '接下来检查并确认下方构建表单。构建成功后，将新镜像标签填入用例的测试镜像或自定义 Agent 镜像，先跑一个小用例，再导出镜像带入内网。构建成功不等于 Agent 和测试一定能运行。',
  'Build form / advanced Dockerfile': '构建表单 / 高级 Dockerfile',
  'Recipe copied. Review and confirm the build below.': '方案已填入，请检查并确认下方构建表单。',
  'Use relative build paths without traversal.': '请使用不包含越界路径的相对文件名。',
  'Use unique NAME=value defaults without credentials.': '请填写不重复的 NAME=value 默认配置，不得包含凭据。',
  'Custom Agent commands cannot use global Pi startup arguments. Put these arguments in the case command instead.': '自定义 Agent 命令不能使用全局 Pi 启动参数，请将参数写在用例的 Agent 命令中。',
};
