# 双 Agent 黑盒可用性验收（2026-09-10）

## 最新结论（2026-09-11）

普通用户角色已通过原生界面自行制作 OpenCode 1.18.30 镜像、配置 MiMo v2.5 环境变量引用、设计后端仓库和独立评分、录入用例，并完成真实 Docker 配对实验 `exp-ec7368d4195c`。无知识库和人工冻结知识库两组均为 **9/9 独立 HTTP 测试通过、0 失败**，`mock=false`、`evidenceIntegrity=verified`、`contextMutated=false`；两组各只有一次运行，不代表统计稳定性或知识库增益。

最终镜像为 `ctxbench/adapted:26b31b1f25d24dee8679f5508cf4a305`，ID `sha256:5908f6a2f0c243c6f7d54d95111b84fba99fbd36de2643c621be53a2aad4e625`。模型调用确有文本、工具调用和结束事件，两组分别 12/11 条 OpenCode `step_finish`，且实际生成源码修复；不能把自定义适配器中未知 Token 统计或 OpenCode 未配置价格的 `cost=0` 解释成无用量/免费。

新资源包导出 `op-b34f7dc7536a4d35` 已完成，包含修正版镜像；普通用户已保存到 `artifacts/ux-opencode-20260910/provider/ux-opencode-20260910-resource-homefix.zip`，协调者核验 Windows 副本的大小与 SHA256 均匹配。最终 ZIP 的 UI 导入前校验 `op-a4f9fb87fc1e4e7c` 已完成，1 镜像/1 基线/1 知识包；未执行覆盖导入。旧 `before-homefix.zip` 只保留作历史失败证据，不是最终可运行交付。UX-001 至 UX-010 均已修复，最后两项日志读取与阅读区域问题也已由普通用户在原生窗口独立复测通过。

本轮没有提交、推送或发布，也没有修改正式服务的数据。以下各阶段为按时间保留的历史记录，早期“尚未调用模型/等待确认”等描述不代表当前状态。

## 角色与隔离

- Avicenna：普通用户视角，不继承开发历史，只获应用用途与验收目标；禁止阅读产品实现、测试脚本、数据库或调用内部 API 绕过页面。可自行读界面帮助、公开使用文档与 OpenCode/MiMo 官方文档，创建自己的后端仓库及判定测试。发现记录在 `findings.md`。
- James：根据体验反馈修复产品代码、交互和文案，补充中英文回归；记录在 `fixes.md`。不替体验角色填写配置或用接口创建通关任务。
- 协调者：准备测试环境、保护凭据/现有数据、串联反馈、部署修复和复核最终证据。

生产服务 `127.0.0.1:48173` 保持不动。测试服务使用独立端口 `48174`、空数据目录和独立容器名 `ctxbench-ux-opencode-20260910-worker`。仅复用已有基础镜像与 Docker Engine，不改基础镜像标签。

MiMo 密钥从已经授权的本机运行时配置直接读入测试服务环境，不在协作消息中复制密钥值，不写入新镜像、用例、报告或命令参数。配置操作只使用允许传入的变量名 `XIAOMI_TOKEN_PLAN_CN_API_KEY`。

本次不提交、不推送、不发布 Release。此前未提交的删除功能及其测试必须保留。

## 初始验收要求

1. 从普通用户界面发现并使用镜像适配功能，以应用提供的基础镜像构建 OpenCode 镜像。
2. 镜像无凭据，默认配置通过环境变量获取 MiMo 中国区 Token Plan API，使用 `mimo-v2.5`。
3. 体验角色自行设计真实小型后端仓库、固定基线 commit、设计能区分基线失败与正确修复通过的判定测试。
4. 经页面创建用例/实验，容器内实际运行 OpenCode，调用真实 Provider；独立评分通过。Mock/接口测试桩不算此项验收。
5. 验证定制镜像可导出打包；记录可复用的产物位置及校验信息，避免源镜像覆盖。
6. 每个问题保留首次期望/操作/观察、修复记录和复测结果，区分产品问题、测试环境问题、外部服务问题。

## 初始化

原生 Windows UI 自动化工具可用。测试服务健康检查成功，runner 为 DockerRunner，MiMo 凭据仅确认 `configured=true`；初始实验/运行/操作数均为零。

发现开发版原生请求固定端口，无法与生产服务同时使用独立数据实例；已作为协调设施事项交 James 实现只在 debug 版本有效的 loopback 端口选择与隔离提示，不记为体验角色首次发现的 UX 问题。

隔离原生窗口现已启动并由 Avicenna 核验：`src-tauri/target/debug/ctxbench-desktop.exe`，独立 WebView2 profile 在 D 盘验收资料目录；进程设置 `CTXBENCH_DEBUG_WORKER_PORT=48174`。测试数据目录为 WSL `/tmp/ctxbench-ux-opencode-20260910-N8UHwU`。后端代码只读挂载，修复需要在本测试实例完全空闲时重启加载，不替换正式镜像。

开始前正式服务基线：59 个实验、234 条运行、58 条操作记录，无活动实验/操作；服务容器启动时间保持 `2026-09-07T01:35:11.909717675Z`。

测试发现范围已修正为仅 `src` 产品测试：原先 Vitest 会误扫验收缓存中的产品副本及外部故意失败的基线。旧全仓测试数不能作为独立产品覆盖数量；此轮以收窄后的产品测试结果为准。

## 首日实测与暂停确认点（历史）

体验角色独立创建了无第三方依赖的 Node.js 库存预留后端，固定基线 `d82fe7a849b150a9149918cc68430aaca5d40d9d`。公开开发测试为 3/4 通过，仓库外的独立 HTTP 验收测试为 5/9 通过，均以非零退出；存在实际业务错误，不是空测试。评分材料不在代码仓或镜像配置中。

已实际发现三个用户体验问题：中文帮助跳英文仓库首页、制作镜像缺少已安装基础镜像选择、文件已载入却仍显示“未选择文件”。前两项产品修改已完成，中文应用内帮助已由体验角色在原生窗口独立复测通过；其他原生复测与第三项修改尚待进行，具体证据见 `findings.md`、`fixes.md`。

体验角色通过原生界面准备了 `ux-opencode-20260910-mimo` 方案：以 `ctxbench/agent-pi:0.1.0` 为基础，从 npm 官方源固定安装 `opencode-ai@1.18.30`，复制仅含环境变量引用的 OpenCode 配置，并设置 `OPENCODE_CONFIG`。尚未点击构建、调用模型或导出镜像。computer-use 的 Windows 安装动作时确认规则要求先取得用户确认；协调者已发出确认问题，未将最初的目标授权冒充此次确认。

协调者最新独立回归：`npm test` 34 文件、204 项通过；`npm run build` 通过（保留单个压缩后 JS 块超过 500 kB 的构建警告）；Rust 原生单测 27 通过、5 项显式标记的 WSL 集成测试未运行；完整 `npm run worker:test` 306 项，其中 2 项因 Windows 符号链接权限跳过，其余通过。这些回归不代表真实 OpenCode 验收已完成。

## 2026-09-11 恢复

用户在收到具体镜像构建确认问题后明确回复“继续啊”，协调者已将真实确认转达体验角色，恢复原两角色分工。仍只授权本次独立 OpenCode 镜像的既定安装方案，不覆盖正式镜像、不发布内容。

恢复前核验：隔离桌面进程 42076、48174 测试容器仍在，测试服务启动时间未变；正式服务启动时间仍为 `2026-09-07T01:35:11.909717675Z`。C 盘可用约 11 GiB，D 盘约 366.8 GiB。此前两个已修复 UX 问题均已由体验角色原生复测通过；待第三项安全热更新及实际构建/模型/导出证据。

### 真实镜像构建已完成

体验角色通过原生界面自行提交 `op-13d9c7bf9a4646ed`，构建完成。新标签为 `ctxbench/adapted:91d5e4cf0966445991274e1a098328e9`，镜像 ID 为 `sha256:0c17af7f94de29ce92b6a0adc8b9507ebe7e917f1f9e1f5888876fa2280f48fa`；UI 日志确认 OpenCode 版本为 1.18.30。基础镜像 ID 与开始前一致，未覆盖原标签。

协调者只读审计已知 MiMo 运行时凭据：检查镜像配置、构建历史、全部保存归档成员（包含 13 个解压后的 gzip 层），约 1.20 GB 解压成员内容；未发现该密钥字节。该检查是针对已知凭据的精确匹配，不冒充通用秘密扫描认证，也没有把压缩归档直接扫描当作层内容扫描。构建镜像仍不代表真实模型评测通过，继续等待用例运行证据。

### 用例录入与后续修复

UX-003 已在镜像提交后的安全点更新，协调者复跑前端 34 文件、207 项测试及构建通过。体验角色新发现 UX-004：用例向导切换步骤后保留旧滚动位置，导致新步骤任务/提示词字段不在视野内；修复角色等用例保存安全点后再更新，不干预当前输入。

体验角色依据页面中的评分说明，自行制作隐藏测试 Git 补丁。协调者只读核验补丁仅新增 `evaluator/acceptance.mjs`，文件 SHA256 仍为原独立评分脚本的 `6d7ce748bb8332f00f5bc7806af25666d227c996cbf656a1a86a9e8fdcf20957`；Agent 原始基线工作树仍干净，没有将评分补丁或参考实现加入 Agent 仓库。

用例已通过 UI 保存为 `case-74b9e65e84124cdbafea2dbd10b1d6d7`，revision 1。协调者只读核验保存后的任务提示和隐藏测试补丁分别与原素材一致，评分命令为 `node evaluator/acceptance.mjs /workspace`，没有参考修复字段；基线及自定义 Agent/评分镜像均与预期一致。

体验角色随后自主探索人工知识包配对，发现 UX-005：实验中的冻结知识库下拉没有匹配包时缺少原因和导入引导。它自行绕到知识库页面导入 `docs/backend-overview.md`（768 字节），冻结包 `645a49662b7ae77b6d4afca15d38df15d2b4eab8fa82a6fa8ca80a51eb692712`，文件 hash `964cd3386b080baafc84920d5989e8b0caa84927456fadbd0b471c20f27b8cbd`。协调者核验其仅含基线的文件组织/语言/接口与现有开发测试事实，没有隐藏测试、Bug 诊断或修复建议。

计划调整为同一个用例的两臂链路验收（无知识库 / 人工冻结知识库，各一次）。该包由用例作者人工提供，不是独立盲法自动生成；本轮目的为接入与因果边界验收，不能依据两条运行作知识库效果或模型优劣结论。UX-005 修改等待本次实验配置保存安全点。

### 第一次真实启动：基线获取失败（未调用模型）

体验角色通过 UI 提交 `exp-efbc54f1cb29`，两臂在获取基线时因 Git `detected dubious ownership` 失败。诊断明确 `agentStarted=false`，没有评分结果；`mock=false` 只说明运行计划为真实执行，不能据此声称已经调用 Provider。实际记录的编码镜像已是新 OpenCode 镜像，两臂 pairingHash 和 promptHash 一致。

协调者只读查明：服务 UID 0；只读挂载的源仓库及 `.git`/HEAD 属于 UID/GID 1000，路径存在且可读。不是目录丢失或需要 chmod。单条只读 Git 命令使用两个明确路径的临时 `safe.directory` 后可读出正确 HEAD；未写全局配置、未改源仓库权限、未代发实验。产品修复与对应错误指引交 James；第一次失败证据保留。

同时记录 UX-006：最终实验摘要显示默认 Pi 镜像而非用例实际覆盖镜像；此为显示问题，不是实跑镜像被覆盖。运行期数据已对照确认。UX-005/006 现在取得安全更新点；体验角色继续独立镜像导出探索，待基线兼容修复后再重跑。

### Git 修复部署及第二次尝试

Git 修复采用针对明确绝对本地仓库的 upload-pack 单命令信任：清空继承的 safe.directory 列表后只增加所选仓库及 `.git`，禁用 hooks，不写全局配置，不更改所有权或原仓库。协调者在同版本一次性 worker 容器执行本地 Git/诊断 16 项测试全部通过。完整 `npm run worker:test` 311 项、308 通过/3 跳过；其中新增 Windows Git 所有权场景的跳过由 Linux 容器实际验证补足，另外两项为 Windows 符号链接权限。

确认没有活动实验/操作后，只重启了 `ctxbench-ux-opencode-20260910-worker`，启动时间变为 `2026-09-11T08:01:45.336719621Z`，48174 恢复 healthy；正式服务仍保持原启动时间。没有替体验者预取基线或发起重跑。

体验者自主创建第二个实验 `exp-b97831342af7`，保留第一个失败实验。两臂同条件且仍使用原镜像，Git 获取和干净基线 checkout 成功，OpenCode Agent 容器实际启动，但两臂均在启动日志目录报 PermissionDenied 后退出；尚不能确认 Provider 调用。体验者在原生日志独立记录为 UX-008，并保留截图。

协调者只读镜像核验：`/home/ctxbench` 属于 UID/GID 10001，但安装/版本检查产生的 `.local/share/opencode/log` 目录链属于 root（755），日志文件尚不存在，运行用户不能创建文件。通用镜像向导只在安装前设置顶层 HOME 所有权，没有安装后的收尾；此问题交修复者通用修复，不给 Agent root 权限、不修改评分测试、不代做用户镜像。修复需要由体验者通过更新后的向导自行产生新派生镜像。

### 资源迁移及 HOME 修正版镜像

体验者自行重试资源导出 `op-e8827c4723664a34` 成功：1 个集合、1 份基线、1 个镜像、1 份上下文，432,068,108 字节；Windows 副本 `provider/ux-opencode-20260910-resource-before-homefix.zip` 的 SHA256 与应用结果一致，为 `b88f2a220362ae55de2e1b57fd47641ebbded9e0fbe35ec6863c51f5ab40e079`。此包含有旧 HOME 问题镜像，只作迁移路径/失败现场证据，不作为最终可运行包；体验者通过 UI 校验操作 `op-36d5db6faad64b76` 也已通过，没有导入覆盖数据。

UX-008 通用修复已在体验者明确安全点更新：安装后只整理默认 HOME 的所有权，拒绝 HOME 根/父 symlink，递归不跟随子链接；构建内以 UID 10001 检查可写/可进入。默认运行用户不变，自定义 HOME/XDG 路径不自动递归处理。公开 UI 说明允许以已装工具镜像为基础、安装命令留空，不必再次下载软件。3 个真实离线一次性容器测试场景通过。

体验者依据新 UI 独立、离线构建 `op-2ff9ab040e4f4ba1`，没有新增文件或重新安装工具。新标签 `ctxbench/adapted:26b31b1f25d24dee8679f5508cf4a305`，ID `sha256:5908f6a2f0c243c6f7d54d95111b84fba99fbd36de2643c621be53a2aad4e625`。协调者在一次性、无网络/无挂载的容器里确认 UID 10001 能在原失败日志目录新建文件，未改变镜像本身；精确已知凭据扫描包含 14 个解压 gzip 层，未发现 MiMo 密钥。仍等待更新用例后的真实模型与评分结果。

最新前端独立回归：35 文件、218 项通过，构建通过；未改后端模块，不需再次重启服务。

### 第三次尝试：真实配对评分成功

体验者仅将同一用例的 Agent 与评分镜像更新为 HOME 修正版，保存为 revision 2，再自行提交 `exp-ec7368d4195c`（`ux-opencode-20260910-mimo-retest2`）。原始任务、隐藏测试、Agent 命令保持不变。配对两臂的基线仍为 `d82fe7a849b150a9149918cc68430aaca5d40d9d`，镜像摘要一致，pairingHash 均为 `cb093d64baca52888bc38f873c94e55eb9001afad98a647f6c52caad059b9a10`，promptHash 均为 `206125f7fc2efe0d02e2fd7c869fc84ed3da2c4c2a89d73cbd3634c75d540404`。

- 人工冻结知识库：`solve-df51c4e9108c4fa0a3bc`，137.006 秒，9 项通过/0 失败；评分日志 SHA256 `215825a51d082f68de73cbfa0d63c09212dd1bf35c52a2c4c89fa3b9aa516897`，源码补丁 SHA256 `bd94bf5accf542d919f841de04ba9c0c1a4a7cac56cdc280af1c4baeaa74d15d`。
- 无知识库：`solve-8261622da0f1414f885a`，150.054 秒，9 项通过/0 失败；评分日志 SHA256 `c4ad55d663b96dd9dc2cc02aa15eb90221e38ad8c918bda9b9f88b64b6d3bcdc`，源码补丁 SHA256 `6c279074596ded48bad1f3e056890c1c125f14b912b82d00e6c054056e78f858`。

两组补丁都实际修改 `src/app.mjs` 与开发测试 `test/reservations.test.mjs`，独立判定使用仓库外隐藏评分脚本，9 项测试没有被 Agent 改写。冻结上下文未变；未注入发现知识库的任务提示。本轮是自创后端的 CTX 风格链路验收，不冒充官方 CTXBench 数据集、自动知识生成或 SWE-SHIELD 约束评测。

最终模型运行后的只读已知凭据检查覆盖隔离目录 331 个文件、1,020,399,764 字节，0 个已知 Key 匹配（存储扫描不展开压缩文件；镜像层另有前述解压扫描）。正式服务仍为 59 个实验/234 条运行/58 条操作、无活动项，启动时间和基础 Pi 镜像 ID 与初始基线一致。

### 最终资源包独立核验

修正版导出文件为 `ctxbench-resources-op-b34f7dc7536a4d35-95104fba.zip`，587,227,148 字节（约 560 MiB），SHA256 `c31168eb9bfe600ef0dda1d8ba19f7c18e0ff4ce0c2e8cd03a00c05379fccad9`。协调者逐个解压读取 ZIP 成员，5 份负载的字节数与 SHA256 全部匹配主 manifest，归档图像 ID 是本轮已跑通的 `5908f6a2…`，上下文文件 hash 与初始冻结版本相同。

资源包声明 `agentCredentialsIncluded=false`，解压后的 ZIP 成员未发现已知 MiMo Key 字节；Docker 归档内部压缩层由前述对应图像 ID 的独立解压审计覆盖。包不包含目标机运行时凭据或 CTXBench 应用安装程序；迁移后需另配运行时密钥。尚未在第二台电脑导入重跑，不把本机导出与校验当作异机兼容验收。

最后再次确认自创基线仓库仍干净、独立评分脚本 SHA256 仍为 `6d7ce748bb8332f00f5bc7806af25666d227c996cbf656a1a86a9e8fdcf20957`，没有为通关改写评分规则。

### 结果阅读阶段的两个后续问题

UX-009 的根因是日志列表对所有 scope 套用简单 ID 规则，拒绝 planner 生成的 `实验:任务:重复:分组` 合法运行 ID。修复只扩展 `benchmarkRunId` 元数据查询的复合格式，保留参数化 SQL、组间过滤和独立归档路径验证；确定性的服务拒绝在前端明确提示、停止无效轮询，网络故障仍可重连。新回归使用真实 planner ID，涵盖中文/点/冒号/长任务 ID、跨组隔离、SQL 字面值和非法路径边界。

确认所有实验/导出空闲后，协调者仅重启 48174 隔离服务，启动时间为 `2026-09-11T08:56:02.488248269Z`。相同两个真实运行 ID 的日志列表均从 HTTP 422 恢复为 HTTP 200，各返回自己的 4 个会话，不串组；无需再次调用模型。

UX-010 的独立原生截图确认评分证据区域被 flex 布局压缩为约一行；修复仅为该证据正文设置防收缩、最小高度和内部滚动，补可聚焦语义。`run-evidence.smoke.mjs` 使用真正 RunDialog，在中英文、850/600 像素高视口测试拒绝后手动恢复、阶段列表、正文不低于 240 像素及键盘 End 滚动到底。前端最终协调者复跑 35 文件/221 项通过、build 通过；仍有单个 JS 块超过 500 kB 的非阻断构建警告。

最终后端全量复跑结束：`npm run worker:test` 共 313 项，310 通过、3 明确跳过，耗时 271.301 秒；跳过仍为 2 个 Windows 符号链接权限和 1 个 Windows 不触发的 Git 所有权检查。后者已有真实 Linux 容器补测通过。`git diff --check` 通过；此前 Rust 27 通过/5 个明确忽略的集成项与 HOME 3 个真实容器场景结果仍适用。

普通用户原生复测已闭环：UX-009 可切换准备、启动校验、编码 Agent 和项目测试阶段，归档首尾导航正常，能读取 OpenCode/MiMo 最终修复输出；UX-010 评分证据恢复约 360 像素多行区，滚动与拖动滚动条可读到 9/9 汇总。截图为 `evidence/ux009-retest-native-log.jpg`、`evidence/ux010-retest-multiline-native.jpg`、`evidence/ux010-retest-scroll-bottom-native.jpg`；两组运行 JSON 也由普通用户通过界面分别导出。

扩展凭据检查明确覆盖隔离服务数据、自创 case/provider/evidence 与本验收报告目录，462 个文件、1,454,235,019 字节，0 个已知 Key 匹配（此时尚未包含后来保存的新 Windows ZIP 副本，副本随后由 SHA256 与已审计导出匹配）。最初把仍运行的 WebView profile 一并读取时发生 I/O 错误，未将失败扫描记为通过；最终排除该锁定浏览器缓存，仅检查明确的交付与评测证据范围。镜像压缩层和 ZIP 校验另见前文，未宣称全机或未知秘密扫描。

收尾后按相同明确范围再次扫描，包含新 Windows ZIP 副本及最终截图/收据：466 个文件、2,041,720,149 字节，0 个已知 Key 匹配。两个角色的记录均已写完，验收协作结束；保留隔离桌面、服务、成功与失败记录供用户查看，没有关闭正式服务、删除现场或追加模型调用。
